parameters:
  kapitan:
    dependencies:
      - type: helm
        source: https://grafana.github.io/helm-charts
        chart_name: grafana
        version: ${grafana-helm:charts:grafana}
        output_path: dependencies/grafana/helmcharts/grafana
    compile:
      - input_paths:
          - grafana-helm/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - grafana-helm/component/main.jsonnet
        input_type: jsonnet
        output_path: grafana/
      - input_type: helm
        input_paths:
          - grafana-helm/helmcharts/grafana
        helm_values: ${grafana-helm:helm_values}
        helm_params:
          namespace: ${grafana-helm:namespace}
        output_path: grafana-helm/01_helmchart
