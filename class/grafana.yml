parameters:
  kapitan:
    compile:
      - input_paths:
          - grafana/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - grafana/component/main.jsonnet
        input_type: jsonnet
        output_path: grafana/

